Perfect — I’ll give you a practical, scalable plan and a Replit-ready pipeline you can run immediately to cover every U.S. county that’s ever been hit by hurricanes or tornadoes — without manually scraping 3,000+ county web pages.

Short version (recommended): don’t try to scrape every county portal. Instead:

Use nationwide parcel/property APIs (Regrid, ATTOM, Estated) as your primary data source — they cover the whole U.S. and return owner/parcel data reliably. 
Regrid
+1

Use NOAA / NCEI Storm Events Database to generate the list of counties affected by hurricanes/tornadoes for any date range (bulk CSV or query). 
NCEI
+1

For any county where the national provider misses data, fall back to county assessor endpoints selectively (high-priority counties only) or request bulk county data directly from the county. For full nationwide scale, Regrid/ATTOM will save you enormous engineering + legal headaches. 
Regrid Support
+1

Below you get everything you can copy into Replit and run now:

a short architecture / process summary,

production cautions (costs, rate limits, privacy),

a Replit-ready Node.js pipeline (storm_to_parcels.js) that:

downloads / parses NOAA storm events (or reads a local CSV you upload),

builds a deduplicated list of (county, state),

resolves county FIPS via the Census API,

gets county geometry via TIGERweb, and

queries Regrid (recommended) for parcels in that county (falls back to ATTOM by address if you prefer).

exact Replit env vars / package.json and how to run it,

citations and next steps.

If you want it, I can run a one-time extraction for a date range you pick (for example: 1990-01-01 → 2025-09-26) and return the full county list — but I will not run background jobs: you (or I right now in this chat) must trigger it. Tell me the date range and I’ll fetch it on demand. (No waiting — I’ll do it in this chat turn if you want.)

Architecture summary (what this pipeline does & why)

Get storm events from NOAA/NCEI (bulk CSV or API). Extract unique county/state pairs that experienced either Hurricane (or related event types: tropical storm, storm surge) and Tornado. 
NCEI
+1

Convert county & state names to FIPS with the Census API (automated). The FIPS lets us request a county geometry reliably. 
Census.gov

Use TIGERweb (Census) to fetch the county polygon or bounding box by FIPS (so we know exact geographic limits). 
Census.gov

Use Regrid Parcel API (recommended) to query parcels within the county geometry (Regrid supports polygon/bbox searches). Regrid returns parcel records (owner name, address, APN, lat/lon) for the entire county — which you can then enrich with Whitepages/identity enrichment (phone/email) if required and allowed. 
Regrid Support
+1

Save results (Parcels + Owners) to a database (Supabase/Postgres) and expose them through your existing contractor proxy endpoints (the Replit servers we already built). This gives you nationwide coverage with one API contract rather than scraping thousands of county sites.

Why this is the right approach:

Nationwide vendors (Regrid/ATTOM/Estated) already normalize data — huge time/cost saver. 
Regrid
+1

NOAA gives you a canonical list of which counties were impacted historically — so you only run parcel pulls for counties that matter. 
NCEI

Production cautions (must read)

Cost & rate limits: pulling parcel records for thousands of counties will generate many API calls and billable rows — expect nontrivial cost. Use batching, daily quotas, and Regrid/ATTOM sales negotiations (bulk licensing) for large runs. 
Regrid Support
+1

PII & compliance: phone/email enrichment and marketing outreach require TCPA / CAN-SPAM compliance and possibly state privacy law considerations. Keep an audit trail of lookups and purpose. If you make adverse decisions (tenant/employment/credit) you must use an FCRA provider. 
Census.gov

County scraping is fragile: some county portals block bots or require login. Use county endpoints only for a small set of high-priority counties or for one-off verifications. Regrid/ATTOM are the right scale solution. 
Regrid Support

Replit-ready pipeline — files to create

Create a Node.js Repl with these files:

package.json

storm_to_parcels.js (main script)

Use Replit Secrets for env vars (see list below).

package.json
{
  "name": "storm-to-parcels",
  "version": "1.0.0",
  "main": "storm_to_parcels.js",
  "scripts": {
    "start": "node storm_to_parcels.js"
  },
  "dependencies": {
    "node-fetch": "^2.6.7",
    "csv-parse": "^5.5.0",
    "jsdom": "^22.1.0",
    "dotenv": "^16.0.3",
    "turf": "^6.5.0" 
  }
}


(We use csv-parse to parse NOAA CSVs, node-fetch for HTTP, turf for simple geometry bbox ops if needed.)

storm_to_parcels.js

Drop this entire file into your Repl. Replace REGRID_API_KEY (or ATTOM keys) in Replit Secrets.

Note: This code is designed to be run by you in Replit. It does network calls (NOAA, Census TIGERweb, Regrid) at runtime. I added defensive fallback paths and comments.

// storm_to_parcels.js
// Replit-ready. Pipeline:
// 1) Download NOAA Storm Events CSV (or read local file)
// 2) Extract deduped county/state pairs for hurricanes & tornadoes
// 3) Resolve state & county FIPS via Census API
// 4) Get county polygon/bbox via TIGERweb
// 5) Query Regrid Parcel API for parcels within the county bbox/polygon
// 6) Save results to JSON files in /tmp (or send to your DB)

require('dotenv').config();
const fetch = require('node-fetch');
const fs = require('fs');
const path = require('path');
const csv = require('csv-parse');
const turf = require('@turf/turf');

const NOAA_CSV_URL = process.env.NOAA_CSV_URL || ''; // optional: if set, will download CSV from NOAA
const LOCAL_NOAA_CSV = process.env.LOCAL_NOAA_CSV || '/tmp/storm_events.csv'; // fallback if you upload CSV
const REGRID_API_KEY = process.env.REGRID_API_KEY || ''; // recommended for nationwide parcels
const ATTOM_API_KEY = process.env.ATTOM_API_KEY || ''; // optional
const START_YEAR = process.env.START_YEAR || '1950';
const END_YEAR = process.env.END_YEAR || '2025';
const OUTPUT_DIR = process.env.OUTPUT_DIR || './output';

if (!fs.existsSync(OUTPUT_DIR)) fs.mkdirSync(OUTPUT_DIR, { recursive: true });

// Utility: download file to local path
async function downloadFile(url, destPath) {
  console.log('Downloading', url);
  const res = await fetch(url);
  if (!res.ok) throw new Error('Download failed: ' + res.status);
  const fileStream = fs.createWriteStream(destPath);
  await new Promise((resolve, reject) => {
    res.body.pipe(fileStream);
    res.body.on('error', reject);
    fileStream.on('finish', resolve);
  });
  console.log('Saved to', destPath);
}

// 1) Obtain NOAA CSV.
// If NOAA_CSV_URL provided, download it. Otherwise, expect LOCAL_NOAA_CSV to exist.
async function ensureNoaaCsv() {
  if (NOAA_CSV_URL) {
    await downloadFile(NOAA_CSV_URL, LOCAL_NOAA_CSV);
  }
  if (!fs.existsSync(LOCAL_NOAA_CSV)) {
    throw new Error('No NOAA CSV found. Set NOAA_CSV_URL or upload CSV to ' + LOCAL_NOAA_CSV);
  }
  return LOCAL_NOAA_CSV;
}

// 2) Parse CSV and extract county/state for tornado & hurricane-like event types.
async function parseNoaaCsv(csvPath) {
  return new Promise((resolve, reject) => {
    const read = fs.createReadStream(csvPath);
    const parser = csv.parse({ columns: true, skip_empty_lines: true });
    const counties = new Set();
    parser.on('readable', () => {
      let record;
      while ((record = parser.read())) {
        // NOAA field names vary by year/format. Common fields: EVENT_TYPE, STATE, CZ_NAME (county)
        const eventType = (record.EVENT_TYPE || record.event_type || '').toLowerCase();
        const state = (record.STATE || record.state || '').trim();
        const cz = (record.CZ_NAME || record.County || record.CountyName || record.cz_name || '').trim();

        if (!state || !cz) continue;

        // match hurricane-related and tornado events:
        if (eventType.includes('tornado') ||
            eventType.includes('hurricane') ||
            eventType.includes('tropical') ||
            eventType.includes('tropical storm') ||
            eventType.includes('storm surge') ||
            eventType.includes('hurricane gust') ) {
          const key = `${cz}||${state}`;
          counties.add(key);
        }
      }
    });
    parser.on('error', reject);
    parser.on('end', () => resolve(Array.from(counties).map(s => {
      const [cz, state] = s.split('||');
      return { countyName: cz, stateName: state };
    })));
    read.pipe(parser);
  });
}

// 3) Use Census API to map stateName -> stateFips and countyName -> countyFips
// We'll fetch all states, then fetch counties for that state and find by name (case-insensitive)
async function getStateFipsMap() {
  // Census API - get all states
  const url = 'https://api.census.gov/data/2020/dec/pl?get=NAME&for=state:*';
  const res = await fetch(url);
  if (!res.ok) throw new Error('Census states fetch failed: ' + res.status);
  const json = await res.json(); // array of arrays: [ ["NAME","state"], ["Alabama","01"], ... ]
  const map = {};
  for (let i=1;i<json.length;i++){
    const [name, fips] = json[i];
    map[name.toLowerCase()] = fips.padStart(2,'0');
  }
  return map;
}

async function getCountyFips(stateFips, countyName) {
  // fetch counties for a state
  const url = `https://api.census.gov/data/2020/dec/pl?get=NAME&for=county:*&in=state:${stateFips}`;
  const res = await fetch(url);
  if (!res.ok) throw new Error('Census counties fetch failed: ' + res.status);
  const json = await res.json();
  for (let i=1;i<json.length;i++){
    const [name, stFips, countyFips] = json[i]; // name contains "County, State"
    // name usually is like "Muscogee County, Georgia"
    // Compare ignoring "County" suffix and case/spacing
    const normalized = name.replace(/county/i,'').replace(/parish/i,'').replace(/borough/i,'').replace(/census area/i,'').trim().toLowerCase();
    const target = countyName.replace(/county/i,'').trim().toLowerCase();
    if (normalized === target || normalized.startsWith(target) || target.startsWith(normalized)) {
      // return 5-digit fips (state+county)
      return stFips.padStart(2,'0') + countyFips.padStart(3,'0');
    }
  }
  return null;
}

// 4) TIGERweb: fetch county geometry (GeoJSON) by FIPS
// TIGERweb GeoServices REST supports queries by FIPS. We'll use the County layer.
// Documentation: https://tigerweb.geo.census.gov/tigerwebmain/TIGERweb_restmapservice.html
async function fetchCountyGeojson(countyFips) {
  const stateFips = countyFips.slice(0,2);
  const countyCode = countyFips.slice(2); // 3-digit
  // Query the TIGERweb County layer: layer 0 typically contains county shapes
  // Build the where clause as STATE='xx' AND COUNTY='yyy'
  const where = `STATE='${stateFips}' AND COUNTY='${countyCode}'`;
  const url = `https://tigerweb.geo.census.gov/arcgis/rest/services/TIGERweb/County/MapServer/1/query?where=${encodeURIComponent(where)}&outFields=*&outSR=4326&f=geojson`;
  const res = await fetch(url);
  if (!res.ok) throw new Error('TIGERweb query failed: ' + res.status);
  const geojson = await res.json();
  // geojson will have features; return first feature's geometry
  if (geojson && geojson.features && geojson.features.length) return geojson.features[0].geometry;
  return null;
}

// 5) Query Regrid for parcels inside bbox or polygon
// Regrid Parcel API supports polygon search. We'll call a generic endpoint.
// See Regrid docs: https://support.regrid.com/api/section/parcel-api
async function fetchParcelsRegridByPolygon(geometry) {
  if (!REGRID_API_KEY) throw new Error('REGRID_API_KEY not set in environment');
  // Convert geometry to minimal bounding box polygon for simplicity (Regrid supports polygon).
  const bbox = turf.bbox(geometry);
  const [minX,minY,maxX,maxY] = bbox;
  // Regrid endpoint (example): GET https://api.regrid.com/v1/parcels/search?bbox=minX,minY,maxX,maxY&api_key=KEY
  const url = `https://api.regrid.com/v1/parcels/search?bbox=${minX},${minY},${maxX},${maxY}&api_key=${encodeURIComponent(REGRID_API_KEY)}`;
  const res = await fetch(url);
  if (!res.ok) {
    const txt = await res.text().catch(()=>String(res.status));
    throw new Error('Regrid request failed: ' + res.status + ' ' + txt);
  }
  return res.json(); // returns parcel items
}

// Save JSON helper
function saveJson(name, obj) {
  const p = path.join(OUTPUT_DIR, name + '.json');
  fs.writeFileSync(p, JSON.stringify(obj, null, 2));
  console.log('Saved', p);
}

// Main runner
(async () => {
  try {
    console.log('Starting pipeline...');
    const csvPath = await ensureNoaaCsv();
    console.log('Parsing NOAA CSV...');
    const countyPairs = await parseNoaaCsv(csvPath);
    console.log('Found', countyPairs.length, 'unique county/state entries (hurricane/tornado).');
    saveJson('noaa_counties_raw', countyPairs);

    // Prepare state FIPS map
    console.log('Fetching state FIPS map from Census...');
    const stateMap = await getStateFipsMap();

    // Resolve counties -> FIPS
    const resolved = [];
    for (const cs of countyPairs) {
      const { countyName, stateName } = cs;
      const stateKey = stateName.trim().toLowerCase();
      const stateFips = stateMap[stateKey];
      if (!stateFips) {
        console.warn('State not found in Census list:', stateName, ' — skipping', countyName);
        continue;
      }
      const countyFips = await getCountyFips(stateFips, countyName);
      if (!countyFips) {
        console.warn('Could not resolve FIPS for', countyName, stateName);
        resolved.push({ countyName, stateName, stateFips, countyFips: null });
        continue;
      }
      console.log('Resolved', countyName, stateName, '-> FIPS', countyFips);
      resolved.push({ countyName, stateName, stateFips, countyFips });
    }
    saveJson('resolved_counties', resolved);

    // For each resolved county, fetch TIGERweb geometry and then Regrid parcels
    for (const r of resolved) {
      if (!r.countyFips) continue;
      console.log('Fetching geometry for', r.countyName, r.stateName, 'FIPS', r.countyFips);
      try {
        const geometry = await fetchCountyGeojson(r.countyFips);
        if (!geometry) {
          console.warn('Geometry not found for', r.countyFips);
          continue;
        }
        saveJson(`geometry_${r.countyFips}`, geometry);

        // Query Regrid parcels by bbox (or polygon)
        console.log('Querying Regrid for parcels in', r.countyFips);
        const parcels = await fetchParcelsRegridByPolygon(geometry);
        console.log('Regrid returned', (parcels && parcels.items && parcels.items.length) || 0, 'parcels (may be paged).');
        saveJson(`parcels_${r.countyFips}`, parcels);
        // NOTE: For large counties the response will be paged or limited. Use Regrid bulk or tile export for big counties.
      } catch (err) {
        console.error('County processing error for', r.countyFips, err.message);
      }
    }

    console.log('Pipeline complete. Output JSON files located in', OUTPUT_DIR);
  } catch (err) {
    console.error('Pipeline failed', err);
  }
})();

Replit / env vars to set (Secrets)

REGRID_API_KEY = <your-regrid-key> (recommended)

ATTOM_API_KEY = <optional-attom-key>

NOAA_CSV_URL = <optional — a NOAA CSV URL you want to download> (or upload CSV to /tmp/storm_events.csv in the Repl)

START_YEAR and END_YEAR optional (used for your CSV naming / filtering if you want to generate per-year CSVs)

OUTPUT_DIR optional (defaults to ./output)

How to run

Create the Repl, copy package.json and storm_to_parcels.js.

Add Replit Secrets (REGRID_API_KEY, etc.).

If you want NOAA to fetch a single CSV for a year range, you can find/download the appropriate CSV from NOAA’s Storm Events FTP/bulk downloads page: https://www.ncei.noaa.gov/stormevents/ftp.jsp
 or manually upload your CSV into the repl path /tmp/storm_events.csv. 
NCEI

Click Run (or npm start) in Replit. The script will:

parse the CSV, build county list, resolve FIPS, pull geometry, call Regrid for parcels, and save JSONs into ./output.

Inspect the JSONs and load into your contractor proxy / database.

Pagination & scale notes

Many counties contain tens or hundreds of thousands of parcels. Regrid responses will be paged or limited. For county-wide exports you should request bulk exports from Regrid (they offer bulk files / feature services) or negotiate a bulk license so you don’t pay per-parcel API costs. 
Regrid Support

For immediate contractor needs (leads), you can restrict to: storm-impacted parcels within an X-mile buffer of the storm track or within N nearest parcels to an address — this dramatically reduces calls and cost.

Why this pipeline avoids scraping every county

It automates county discovery (via NOAA) and uses one nationwide parcel API for retrieval. That means you don’t have to research or maintain 3,000 county portals. You only hit county portals selectively when the national provider misses records for a specific county — which is rare. This is a far more reliable, maintainable approach that scales. 
Regrid
+1

Citations / docs (so you or your Replit dev team can register)

NOAA / NCEI Storm Events Database (bulk CSVs & docs). 
NCEI
+1

Regrid Parcel API (nationwide parcel search, polygon & bbox search). 
Regrid Support
+1

ATTOM property API (enterprise property data). 
ATTOM Developer Platform
+1

Estated Property API v4 docs (alternate provider, supports FIPS/APN queries). 
Estated
+1

Census TIGERweb GeoServices (county geometry by FIPS). 
Census.gov

Next steps I can do right now in this chat

Pick one and I’ll execute it immediately (and return the result in this reply):

A) Run the NOAA → county extraction for a date range you choose (e.g., 1990-01-01 → 2025-09-26) and return the deduplicated county list (CSV + JSON). (I’ll fetch NOAA and produce the list here.)
B) Generate the Replit project as a zip with package.json + storm_to_parcels.js so you can download & run it.
C) Start pulling parcels for a smaller sample (for example: all counties that had storms in 2020) using your Regrid key (you must paste the key or set it in Replit; if you prefer, I’ll show you the exact curl commands to run locally).
D) Produce the exact vendor sales email templates for Regrid/ATTOM/Estated asking for bulk county export pricing and sample API keys.

Tell me which (A/B/C/D) and I’ll run or produce it now. If you want A, give me the date range you want (start & end).