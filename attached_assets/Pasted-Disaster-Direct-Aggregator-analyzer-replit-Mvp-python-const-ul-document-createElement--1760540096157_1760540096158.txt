Disaster Direct – Aggregator/analyzer (replit Mvp)
· python
"""
  const ul = document.createElement('ul');
  (j||[]).forEach(it=>{
    const li = document.createElement('li');
    li.innerHTML = `<strong>#${it.id}</strong> ${new Date(it.created_at*1000).toLocaleString()} — ${it.name} — risk ${it.risk_score} `+
      `[ <a target="_blank" href="/runs/${it.id}">JSON</a> | <a target="_blank" href="/runs/${it.id}/events.csv">events.csv</a> | <a target="_blank" href="/runs/${it.id}/impacted.csv">impacted.csv</a> ]`;
    ul.appendChild(li);
  });
  el.appendChild(ul);
}


async function doGeocode(){
  const q = document.getElementById('addr').value;
  if(!q) return;
  const r = await fetch('/geocode?q='+encodeURIComponent(q));
  const j = await r.json();
  if(j && j.lat && j.lon){
    document.getElementById('lat').value = j.lat;
    document.getElementById('lon').value = j.lon;
    map.setView([parseFloat(j.lat), parseFloat(j.lon)], 10);
  } else {
    alert('No result');
  }
}
function openParcelReport(id){
  const lat = document.getElementById('lat').value;
  const lon = document.getElementById('lon').value;
  const rad = document.getElementById('rad').value;
  const hrs = document.getElementById('hrs').value;
  window.open(`/report/parcel/${id}?lat=${lat}&lon=${lon}&radius_km=${rad}&hours_back=${hrs}`,'_blank');
}


function exportCSV(type){
  const lat = document.getElementById('lat').value;
  const lon = document.getElementById('lon').value;
  const rad = document.getElementById('rad').value;
  const hrs = document.getElementById('hrs').value;
  const url = type==='events'
    ? `/export/events.csv?lat=${lat}&lon=${lon}&radius_km=${rad}&hours_back=${hrs}`
    : `/export/impacted.csv?lat=${lat}&lon=${lon}&radius_km=${rad}&hours_back=${hrs}`;
  window.open(url, '_blank');
}


// Map click -> set lat/lon and reverse geocode
setTimeout(()=>{
  if(map){
    map.on('click', async (e)=>{
      const {lat, lng} = e.latlng;
      document.getElementById('lat').value = lat.toFixed(6);
      document.getElementById('lon').value = lng.toFixed(6);
      try{
        const r = await fetch(`/reverse_geocode?lat=${lat}&lon=${lng}`);
        const j = await r.json();
        if(j && j.display_name){
          document.getElementById('addr').value = j.display_name;
        }
      }catch(err){ console.log(err); }
    });
  }
}, 500);
function startDrawZone(){
  // Click the polygon tool in the toolbar; helper provided for UX naming
  alert('Use the polygon/rectangle tool in the top-lef

Boom — shipped all seven upgrades. Here’s what you’ve got now:

What I added

Run all schedules at once

New button above the list: Run all schedules now (calls GET /cron/run and dumps results in the output panel).

One-click Clone for schedules

Each job row has a clone link.

Server route: POST /schedules/{id}/clone (duplicates the job with “(copy)” appended).

Export schedules CSV

Button next to “Run all”: Export schedules CSV → downloads via GET /schedules.csv.

PDF pack (zip) after a run

Server route: GET /runs/{run_id}/pdfs.zip.

For now it zips a summary JSON + events.csv + impacted.csv (MVP-friendly; later we can switch to real PDFs per parcel).

Live radar overlay

Radar tiles auto-enable shortly after map init (IEM NEXRAD cache).

You’ll see radar on the map by default; we can expose a checkbox later if you want to toggle.

NWS severe alerts feed

Button added functionally (no visible button change required by you): call loadNWS() from console or wire to UI.

Server route: GET /nws.geojson (proxies active alerts from api.weather.gov).

Layer styling is red outlines; popups show the alert type.

Lightning & Wildfire adapters (stubs)

GET /adapters/lightning and GET /adapters/wildfire return the expected GeoJSON schema so a provider can post compatible data into your pipeline later.

Quick how-to

Run all: click Run all schedules now → see JSON results in the panel.

Clone a job: click clone beside any job → a duplicate appears in the list.

Export jobs: click Export schedules CSV to download all schedule settings.

Per-run zip: After a schedule Test (or Run Now + Save), grab …/runs/{id}/pdfs.zip (we can also add a UI link next to the per-run CSVs if you want).

NWS alerts: run loadNWS() (I can also add a visible “Load NWS Alerts” button in the map toolbar if you’d like it persistent).

Radar: loads automatically on map; if you prefer a toggle, I can add a checkbox control.

Next polish (tell me which to do)

Add a visible “Load NWS Alerts” button on the map header.

Add a Radar checkbox to toggle the layer on/off.

Wire a “Download PDF Pack” link into the Schedule Test output next to the CSV links.

Switch the PDF pack to real PDFs for each parcel (I can implement a lightweight HTML→PDF builder if your environment supports it).